---
"pi-cliproxy-usage": minor
---

Add per-account enable/disable toggles in `/cliproxy-usage settings`, so you can pick which discovered accounts to use within an enabled provider instead of only enabling or disabling an entire provider.
